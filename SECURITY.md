# 安全策略

## 支持范围

项目处于 `0.x` 阶段。安全修复只保证进入最新的 `main` 和最新发布版本；尚未承诺维护旧的次要版本分支。

## 私下报告漏洞

请不要为以下问题创建公开 Issue、PR 或 Discussion：

- Key、Token、会话 Secret 或其他凭据泄露；
- 鉴权、匿名会话隔离、级联删除或限流绕过；
- 精确位置、路线或用户数据意外暴露；
- SSRF、路径遍历、命令执行、SQL 注入、XSS 或供应链风险；
- 可造成第三方 Provider 配额或费用滥用的问题。

仓库公开后，首选使用 GitHub 的 **Private vulnerability reporting**。在该功能启用前，请通过维护者 [@Harvey-015](https://github.com/Harvey-015) 个人资料中的私下联系方式发送报告。不要在报告中附带真实用户数据或仍然有效的生产凭据；如需证明影响，请使用已撤销的测试凭据和最小复现。

报告应尽量包含：

- 受影响版本或 commit SHA；
- 风险类型和可能影响；
- 不包含 Secret 的复现步骤；
- 建议修复或缓解方式；
- 是否已经向第三方 Provider 报告。

维护者会确认收到报告、评估严重程度、准备修复并协调披露时间。具体响应时间取决于项目维护能力，当前不提供商业 SLA。

## 安全开发要求

- 真实 Secret 只能存储在本地忽略文件或部署平台 Secret 管理中；
- 公开前和每个 PR 都应运行 `pnpm run security:static` 与 `pnpm run security:history`；
- Provider 在线测试必须受请求数量和额度确认保护；
- 高德 JS 安全密钥保持服务端代理，不得改为生产前端明文；
- 影响数据处理、导出、缓存或持久化的变更必须同步合规文档。
